name: first_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: ^0.9.2
  provider: ^6.0.0
  flip_card: ^0.5.0
  multi_select_flutter: ^4.0.0
  flutter_custom_dialog: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/card_back.png"

flutter:
  uses-material-design: true
  assets:
    - assets/card_back.png
    